diff --git a/src/include/sys/wait.h b/src/include/sys/wait.h
new file mode 100644
index 0000000000..0e0299d6eb
--- /dev/null
+++ b/src/include/sys/wait.h
@@ -0,0 +1,32 @@
+#ifndef PGLITE_WASI_SYS_WAIT_H
+#define PGLITE_WASI_SYS_WAIT_H
+
+#if defined(__wasi__)
+#include <errno.h>
+#include <sys/types.h>
+
+#ifndef WNOHANG
+#define WNOHANG 1
+#endif
+
+#define WIFEXITED(status) (0)
+#define WEXITSTATUS(status) (0)
+#define WIFSIGNALED(status) (0)
+#define WTERMSIG(status) (0)
+#define WIFSTOPPED(status) (0)
+#define WSTOPSIG(status) (0)
+#define WIFCONTINUED(status) (0)
+
+static inline pid_t
+waitpid(pid_t pid, int *status, int options)
+{
+	(void) pid;
+	(void) status;
+	(void) options;
+	return 0;
+}
+#else
+#include_next <sys/wait.h>
+#endif
+
+#endif
